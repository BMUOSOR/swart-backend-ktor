ktor {
    deployment {
        port = 8080
        port = ${?PORT}
    }
    application {
        modules = [ com.swart.api.ApplicationKt.module ]
    }
}

storage {
    jdbcUrl = ${?DB_JDBC_URL}
    dbUser = ${?DB_USER}
    dbPassword = ${?DB_PASSWORD}
}
